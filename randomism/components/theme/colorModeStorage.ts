/** localStorage key for MUI ThemeProvider `mode` (`light` | `dark` | `system`). */
export const COLOR_MODE_STORAGE_KEY = "randomism-color-mode";

/** DOM attribute set by InitColorSchemeScript + CSS var selectors. */
export const COLOR_MODE_ATTR = "data-color-mode";

/** Companion key used by MUI for per-scheme overrides (keep stable). */
export const COLOR_SCHEME_STORAGE_KEY = "randomism-color-scheme";
